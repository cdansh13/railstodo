<%= form_with(model: todo_list) do |form| %>
  <% if todo_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      <ul>
        <% todo_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">

    <form class="form-control">
      <!-- <h1>New todo list</h1> -->
      <div>
        <%= form.label :title, style: "display: block", class:"form-label" %>
        <%= form.text_field :title ,class:"form-control"%>
      </div>
      
      <div>
        <%= form.label :desc, style: "display: block", class:"form-label" %>
        <%= form.text_field :desc, class:"form-control" %>
      </div>
      <div>
        <%= form.number_field :user_id, id: :todo_list_user_id, class:"form-control", value: current_user.id, type: :hidden %>
        </div>
      
      <div class="my-2">
        <%= form.submit class:"btn btn-secondary"%>
        <!-- <%= link_to "Back to Todo list", todo_lists_path, class:"btn btn-secondary" %> -->
        
      </div>
    </form>
    <% end %>
  </div>
    